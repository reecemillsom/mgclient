language: node_js

services:
  - docker
  - mongodb

before_install:
  - docker pull mongo
  - docker run -d -p 127.0.0.27017:4567 mongo
  - docker container ls

install:
  - npm install
script:
  - yarn test
  - codecov
